import FormLogin from "./features/login/components/FormLogin"

function App() {
  return (
    <main className="flex flex-col gap-2 h-screen w-screen items-center justify-center bg-(--bg) text-(--text) p-2">
      <FormLogin/>
    </main>
  )
}

export default App
